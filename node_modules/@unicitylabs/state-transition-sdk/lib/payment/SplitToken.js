/**
 * Realized split output: all data needed to mint the new token.
 */
export class SplitToken {
    networkId;
    recipient;
    tokenType;
    salt;
    assets;
    proofs;
    constructor(networkId, recipient, tokenType, salt, assets, proofs) {
        this.networkId = networkId;
        this.recipient = recipient;
        this.tokenType = tokenType;
        this.salt = salt;
        this.assets = assets;
        this.proofs = proofs;
    }
}
//# sourceMappingURL=SplitToken.js.map