import { DataHash } from '../../crypto/hash/DataHash.js';
/**
 * Finalized leaf in a sparse Merkle sum tree.
 */
export declare class FinalizedLeafBranch {
    readonly path: bigint;
    private readonly _data;
    readonly value: bigint;
    readonly hash: DataHash;
    constructor(path: bigint, _data: Uint8Array, value: bigint, hash: DataHash);
    /**
     * @returns {Uint8Array} Copy of the leaf data bytes.
     */
    get data(): Uint8Array;
    /**
     * @returns {Promise<FinalizedLeafBranch>} This branch (already finalized).
     */
    finalize(): Promise<FinalizedLeafBranch>;
    /**
     * @returns {string} String representation of the leaf.
     */
    toString(): string;
}
