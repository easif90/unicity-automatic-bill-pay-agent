import { FinalizedBranch } from './FinalizedBranch.js';
import { DataHash } from '../../crypto/hash/DataHash.js';
/**
 * Finalized interior node in a plain sparse Merkle tree.
 */
export declare class FinalizedNodeBranch {
    readonly path: bigint;
    readonly left: FinalizedBranch;
    readonly right: FinalizedBranch;
    readonly hash: DataHash;
    constructor(path: bigint, left: FinalizedBranch, right: FinalizedBranch, hash: DataHash);
    /**
     * @returns {Promise<FinalizedNodeBranch>} This branch (already finalized).
     */
    finalize(): Promise<FinalizedNodeBranch>;
    /**
     * @returns {string} String representation of the node.
     */
    toString(): string;
}
