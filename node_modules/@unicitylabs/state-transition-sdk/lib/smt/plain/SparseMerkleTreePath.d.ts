import { PathVerificationResult } from '../PathVerificationResult.js';
import { SparseMerkleTreePathStep } from './SparseMerkleTreePathStep.js';
import { DataHash } from '../../crypto/hash/DataHash.js';
/**
 * Path through a plain sparse Merkle tree from a leaf to the root.
 */
export declare class SparseMerkleTreePath {
    readonly root: DataHash;
    readonly steps: ReadonlyArray<SparseMerkleTreePathStep>;
    constructor(root: DataHash, steps: ReadonlyArray<SparseMerkleTreePathStep>);
    /**
     * Create SparseMerkleTreePath from CBOR bytes.
     *
     * @param {Uint8Array} bytes CBOR bytes.
     * @returns {SparseMerkleTreePath} Decoded path.
     */
    static fromCBOR(bytes: Uint8Array): SparseMerkleTreePath;
    /**
     * Convert SparseMerkleTreePath to CBOR bytes.
     *
     * @returns {Uint8Array} CBOR bytes.
     */
    toCBOR(): Uint8Array;
    /**
     * @returns {string} String representation of the path.
     */
    toString(): string;
    /**
     * Verifies the tree path against the root hash and state ID.
     * @param stateId The state ID as bit string in bigint format to verify against the path.
     * @returns A Promise resolving to a PathVerificationResult indicating success or failure.
     */
    verify(stateId: bigint): Promise<PathVerificationResult>;
}
