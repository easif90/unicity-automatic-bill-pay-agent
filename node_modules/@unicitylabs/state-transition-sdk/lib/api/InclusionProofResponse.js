import { InclusionProof } from './InclusionProof.js';
import { CborDeserializer } from '../serialization/cbor/CborDeserializer.js';
import { CborSerializer } from '../serialization/cbor/CborSerializer.js';
/**
 * Inclusion proof response.
 */
export class InclusionProofResponse {
    blockNumber;
    inclusionProof;
    /**
     * Create inclusion proof response.
     *
     * @param inclusionProof inclusion proof
     * @param blockNumber block number
     */
    constructor(blockNumber, inclusionProof) {
        this.blockNumber = blockNumber;
        this.inclusionProof = inclusionProof;
        this.inclusionProof = inclusionProof;
    }
    /**
     * Create response from CBOR bytes.
     *
     * @param bytes CBOR bytes
     * @return inclusion proof response
     */
    static fromCBOR(bytes) {
        const data = CborDeserializer.decodeArray(bytes, 2);
        return new InclusionProofResponse(CborDeserializer.decodeUnsignedInteger(data[0]), InclusionProof.fromCBOR(data[1]));
    }
    /**
     * Convert InclusionProofResponse to CBOR bytes.
     *
     * @returns {Uint8Array} CBOR bytes.
     */
    toCBOR() {
        return CborSerializer.encodeArray(CborSerializer.encodeUnsignedInteger(this.blockNumber), this.inclusionProof.toCBOR());
    }
}
//# sourceMappingURL=InclusionProofResponse.js.map