import { ShardId } from './ShardId.js';
/**
 * Shard tree certificate.
 */
export declare class ShardTreeCertificate {
    private readonly _shard;
    private readonly _siblingHashList;
    static readonly CBOR_TAG = 39003n;
    private static readonly VERSION;
    constructor(_shard: ShardId, _siblingHashList: Uint8Array[]);
    /**
     * Get the shard.
     */
    get shard(): ShardId;
    /**
     * Get the sibling hash list.
     */
    get siblingHashList(): Uint8Array[];
    /**
     * @returns {bigint} Wire-format version of this certificate.
     */
    get version(): bigint;
    /**
     * Create shard tree certificate from CBOR bytes.
     *
     * @param bytes CBOR bytes
     * @return shard tree certificate
     */
    static fromCBOR(bytes: Uint8Array): ShardTreeCertificate;
    /**
     * Convert shard tree certificate to CBOR bytes.
     *
     * @return CBOR bytes
     */
    toCBOR(): Uint8Array;
    /**
     * Returns a string representation of the ShardTreeCertificate.
     * @returns The string representation.
     */
    toString(): string;
}
