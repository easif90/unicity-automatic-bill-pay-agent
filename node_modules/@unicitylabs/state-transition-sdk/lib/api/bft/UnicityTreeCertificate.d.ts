/**
 * Hash step in the certificate.
 */
declare class HashStep {
    readonly key: bigint;
    private readonly _hash;
    constructor(key: bigint, _hash: Uint8Array);
    /**
     * Get the hash.
     */
    get hash(): Uint8Array;
    /**
     * Create hash step from CBOR bytes.
     *
     * @param bytes CBOR bytes
     * @return hash step
     */
    static fromCBOR(bytes: Uint8Array): HashStep;
    /**
     * Convert hash step to CBOR bytes.
     *
     * @return CBOR bytes
     */
    toCBOR(): Uint8Array;
    /**
     * Returns a string representation of the UnicityTreeCertificate.
     * @returns The string representation.
     */
    toString(): string;
}
/**
 * Unicity tree certificate.
 */
export declare class UnicityTreeCertificate {
    readonly partitionIdentifier: bigint;
    private readonly _steps;
    static readonly CBOR_TAG = 39004n;
    private static readonly VERSION;
    constructor(partitionIdentifier: bigint, _steps: HashStep[]);
    /**
     * Get the steps.
     */
    get steps(): HashStep[];
    /**
     * @returns {bigint} Wire-format version of this certificate.
     */
    get version(): bigint;
    /**
     * Create certificate from CBOR bytes.
     *
     * @param bytes CBOR bytes
     * @return certificate
     */
    static fromCBOR(bytes: Uint8Array): UnicityTreeCertificate;
    /**
     * Convert certificate to CBOR bytes.
     *
     * @return CBOR bytes
     */
    toCBOR(): Uint8Array;
    /**
     * Returns a string representation of the UnicityTreeCertificate.
     * @returns The string representation.
     */
    toString(): string;
}
export {};
